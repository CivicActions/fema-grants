[D8] SignatureField allows you to collect HTML5 canvas based signatures in content type. 
Currently it displays the collected signature as a image and in the back end it stores it as an BLOB. 
The D7 version of the module is it in https://www.drupal.org/project/signaturefield